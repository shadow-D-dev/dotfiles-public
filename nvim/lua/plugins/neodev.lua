-- gives you some lua & neovim autocompletions & lsp features
return {
  "folke/neodev.nvim",
  enabled = false,
  -- opts = {},
}
-- got popup below so disabling this atm

-- Please disable `neodev.nvim` in your config.
--This is no longer needed when you use `lazydev.nvim`
